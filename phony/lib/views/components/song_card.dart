import 'package:flutter/material.dart';

class SongCard extends StatelessWidget{
  const SongCard({super.key}); // TODO: Pass a Song Model to the card

  @override
  Widget build(BuildContext context) {
    return 
  }
}